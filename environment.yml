name: telemetry_ai
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - pandas
  - numpy
  - scikit-learn
  - matplotlib
  - pip:
      - streamlit
      - plotly
      - pytest
      - joblib